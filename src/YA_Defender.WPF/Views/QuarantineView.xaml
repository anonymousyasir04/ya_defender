<UserControl x:Class="YA_Defender.WPF.Views.QuarantineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgBrush}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="QUARANTINE" FontSize="22" FontWeight="Bold" />
            <TextBlock Text="AES-256 encrypted vault - suspicious files stored safely, never executed"
                       Style="{StaticResource SubtitleStyle}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,14,0,10">
            <Button Content="Restore All" Style="{StaticResource GhostButton}" Command="{Binding RestoreAllCommand}" Margin="0,0,8,0" />
            <Button Content="Delete All" Style="{StaticResource DangerButton}" Command="{Binding DeleteAllCommand}" Margin="0,0,8,0" />
            <Button Content="Export CSV" Style="{StaticResource GhostButton}" Command="{Binding ExportCommand}" Margin="0,0,8,0" />
            <Button Content="Refresh" Style="{StaticResource GhostButton}" Command="{Binding RefreshCommand}" />
            <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" Margin="14,0,0,0"
                       Foreground="{StaticResource AccentBrush}" FontSize="12" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.6*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,6,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="QUARANTINED FILES" Style="{StaticResource CardTitleStyle}" />
                    <ListView ItemsSource="{Binding Items}">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding" Value="8,8" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" FontSize="12" />
                                        <TextBlock Text="{Binding OriginalPath}" FontSize="10"
                                                   Foreground="{StaticResource TextDimBrush}" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding ThreatType}" FontSize="11"
                                               Foreground="{StaticResource DangerBrush}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="2" Text="{Binding RiskScore, StringFormat=Risk {0}/100}"
                                               FontSize="11" Foreground="{StaticResource HighlightBrush}" VerticalAlignment="Center" />
                                    <Button Grid.Column="3" Content="Restore" Style="{StaticResource GhostButton}" Padding="8,4" FontSize="11"
                                            Command="{Binding DataContext.RestoreCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}" Margin="4,0" />
                                    <Button Grid.Column="4" Content="Delete" Style="{StaticResource DangerButton}" Padding="8,4" FontSize="11"
                                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="6,0,0,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="NETWORK DNS QUARANTINE" Style="{StaticResource CardTitleStyle}" />
                    <TextBlock DockPanel.Dock="Top" Style="{StaticResource SubtitleStyle}"
                               Text="Block malicious domains in the Windows hosts file to stop data exfiltration." />
                    <Grid DockPanel.Dock="Top" Margin="0,10,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding DnsInput, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Enter a domain, e.g. evil.example.com" />
                        <Button Grid.Column="1" Content="Block" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0"
                                Command="{Binding AddDomainCommand}" />
                    </Grid>
                    <Button DockPanel.Dock="Top" Content="Apply Default Blocklist" Style="{StaticResource GhostButton}"
                            HorizontalAlignment="Left" Padding="12,6" FontSize="11"
                            Command="{Binding ApplyBlocklistCommand}" Margin="0,0,0,8" />
                    <ListBox ItemsSource="{Binding BlockedDomains}" Background="Transparent" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding}" FontSize="12" FontFamily="Consolas"
                                               Foreground="{StaticResource DangerBrush}" />
                                    <Button Grid.Column="1" Content="Unblock" Style="{StaticResource GhostButton}"
                                            Padding="8,2" FontSize="10"
                                            Command="{Binding DataContext.RemoveDomainCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
